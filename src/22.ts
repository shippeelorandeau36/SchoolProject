function getRandomInt(max: number): number {
    return Math.floor(Math.random() * max);
}

// Example usage in a TypeScript file (e.g., index.ts)
const maxValue = 100;
console.log(getRandomInt(maxValue));
